services:
  poker-decision-agent:
    build: .
    image: poker-decision-agent:latest
    ports:
      - "8000:8000"
    environment:
      POKER_POLICY_PATH: /app/models/poker_policy.json
    restart: unless-stopped
